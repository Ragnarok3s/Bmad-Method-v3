apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: bmad-method-dev
resources:
  - ../base
secretGenerator:
  - name: core-app
    behavior: replace
    literals:
      - CORE_DATABASE_URL=postgresql+psycopg://core:core@core-db:5432/core
      - CORE_ALLOWED_ORIGINS=http://localhost:3000,https://dev.bmad-method.local
  - name: frontend-env
    behavior: replace
    literals:
      - NEXT_PUBLIC_CORE_API_URL=https://api.dev.bmad-method.local
  - name: core-database
    behavior: replace
    literals:
      - POSTGRES_DB=core
      - POSTGRES_USER=core
      - POSTGRES_PASSWORD=core
images:
  - name: ghcr.io/bmad-method/backend
    newTag: dev
  - name: ghcr.io/bmad-method/frontend
    newTag: dev
patchesStrategicMerge:
  - ingress-frontend.yaml
  - ingress-backend.yaml
