apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: identity
spec:
  rules:
    - host: identity.dev.bmad-method.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: identity
                port:
                  number: 8100
