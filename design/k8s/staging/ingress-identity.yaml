apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: identity
spec:
  rules:
    - host: identity.staging.bmad-method.internal
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: identity
                port:
                  number: 8100
