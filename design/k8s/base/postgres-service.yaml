apiVersion: v1
kind: Service
metadata:
  name: core-db
  labels:
    app.kubernetes.io/name: core-db
    app.kubernetes.io/component: database
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: core-db
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
